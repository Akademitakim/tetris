using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerInput : MonoBehaviour
{/*
    public bool solaIlerle => Input.GetKeyDown(KeyCode.A) || Input.GetKeyDown(KeyCode.LeftArrow);
    public bool sagaIlerle => Input.GetKeyDown(KeyCode.D) || Input.GetKeyDown(KeyCode.RightArrow);



    private void Update()
    {
        if (solaIlerle || sagaIlerle)
        {
            var deger = solaIlerle ? -1 : 1;
            yatayHareket(deger);
        }
    }

    private void yatayHareket(int deger)
    {
        var current = GameManager.Instance.Current.transform;
        var position = current.position;
        position.x += deger;
        current.position = position;
    }
}*/
}
